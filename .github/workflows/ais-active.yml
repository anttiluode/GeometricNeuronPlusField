name: ais-active-boundary

on:
  workflow_dispatch:
  push:
    paths:
      - 'ais_active_probe.py'
      - 'ais_active_probe_v02.py'
      - 'AIS_ACTIVE_PREREG_V01.md'
      - 'AIS_ACTIVE_PREREG_V02.md'
      - '.github/workflows/ais-active.yml'

jobs:
  ais:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout GeometricNeuronPlusField
        uses: actions/checkout@v4

      - name: Checkout FunctionalArbors
        uses: actions/checkout@v4
        with:
          repository: anttiluode/FunctionalArbors
          path: FunctionalArbors

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install -r requirements.txt

      - name: Active boundary selftest
        run: python ais_active_probe.py --selftest

      - name: Active boundary v0.2 canonical run
        run: >-
          python ais_active_probe_v02.py
          --functional-arbors FunctionalArbors
          --seed-start 0
          --seeds 24
          --lag 20
          --task-steps 180
          --freq-steps 640
          --burn 160
          --min-events 4
          --upper-min-freq 0.05
          --out runs/ais_active_v02/ais_active_v02.json

      - name: Upload active-boundary v0.2 receipt
        uses: actions/upload-artifact@v4
        with:
          name: ais-active-boundary-v02-24
          path: runs/ais_active_v02/
